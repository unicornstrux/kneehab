# Diet Tab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a 🥩 Diet reference card as a new day-type button in the Today tab, containing protein targets, supplement stack, timing rules, and recovery factors.

**Architecture:** New BLOCKS entry `'N'` (Nutrition) with steps array — one step per content section (Protein, Daily Stack, Timing, Cold Exposure, Heat). New day-selector button. No new pages, no new JS functions — uses existing `renderBlock()` pipeline.

**Tech Stack:** Single-file HTML/CSS/JS (index.html)

---

### Task 1: Add the day-selector button

**Files:**
- Modify: `index.html` — day-selector button row (around line 1601)

- [ ] **Step 1: Add the 🥩 button after the ⛷️ Activity button**

Insert between the Activity (`X`) button and Daily Habits (`D`) button:

```html
  <button class="day-btn" data-block="N" onclick="selectBlock('N',this)">
    <span class="day-btn-num">🥩</span>
  </button>
```

The full button row order should be: A, B, 🧘 R, ⛷️ X, 🥩 N, ✨ D

- [ ] **Step 2: Verify in browser**

Open index.html, confirm 🥩 button appears in the selector row. Clicking it should clear the card (no BLOCKS entry yet).

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "add diet day-selector button"
```

---

### Task 2: Add the BLOCKS 'N' entry

**Files:**
- Modify: `index.html` — BLOCKS object, insert before `'D'` entry (around line 2905)

- [ ] **Step 1: Add the full BLOCKS entry**

Insert immediately before `'D': {` in the BLOCKS object:

```javascript
  'N': {
    type:'N', title:'DIET & SUPPLEMENTS', titleClass:'', stepLabel:'Reference',
    pills:[{t:'Daily',c:'green'},{t:'Reference',c:''},{t:'No tracking',c:''}],
    note:'Protein targets, supplement stack, timing rules, and recovery factors. Not a training day — reference card for daily nutrition.',
    steps:[
      {id:'sN_prot', time:'Daily', title:'Protein',                         sub:'140–180g per day',
       detail:[{n:'Protein Target',rx:'140–180g / day',d:'Distribute across meals — ~35-45g per feeding to maximise muscle protein synthesis (Attia). Four meals is ideal. Quality sources: meat, fish, eggs, dairy.'},
               {n:'Collagen Spacing',rx:'2+ hr gap',d:'Keep 2+ hours between any protein meal and collagen dose. Dietary amino acids (especially leucine) compete with collagen peptides for absorption and blunt the connective tissue synthesis signal.'}]},
      {id:'sN_stack', time:'Daily', title:'Daily Stack',                    sub:'Collagen 15g + Vit C 200mg + Creatine 5g',
       detail:[{n:'Collagen + Vitamin C',rx:'15g collagen + 200mg vit C',d:'Connective tissue synthesis (Baar / Shaw 2017). Hydrolyzed collagen provides glycine, proline, hydroxyproline — rate-limiting substrates for tendon and ligament remodelling. Vitamin C is a required cofactor for collagen crosslinking.'},
               {n:'Creatine Monohydrate',rx:'5g / day',d:'Most evidence-backed supplement in sports science. Increases phosphocreatine stores — more force output, faster recovery between sets. Timing does not matter — just daily consistency. Can be taken with collagen, no absorption interaction.'},
               {n:'Day A/B Timing',rx:'45–60 min before training',d:'Take the full stack 45–60 min before mechanical loading. Peak circulating hydroxyproline aligns with the training stimulus — maximises collagen synthesis at the loaded tissue.'},
               {n:'Activity Day Timing',rx:'45–60 min before heading out',d:'Same window as Day A/B. The warm-up fills the gap.'},
               {n:'Rest Day Timing',rx:'Before bed',d:'No training stimulus — target the nocturnal GH pulse instead. GH drives IGF-1 locally in connective tissue, upregulating collagen synthesis overnight. This window is training-independent — it happens every night.'}]},
      {id:'sN_time', time:'Daily', title:'Timing Rules',                    sub:'Meal spacing and sleep',
       detail:[{n:'Stop Eating Before Bed',rx:'2–3 hrs before sleep',d:'Late eating blunts the nocturnal GH pulse (Baar\'s second connective tissue synthesis window) and degrades sleep quality. Metabolic health benefit (Attia).'},
               {n:'Rest Day Exception',rx:'Collagen before bed is OK',d:'Small collagen dose is not a meal — does not trigger the insulin response that suppresses GH. Take it as the last thing before bed on rest days.'}]},
      {id:'sN_cold', time:'Day A/B', title:'Cold Exposure',                 sub:'Avoid within 4–6 hrs of training',
       detail:[{n:'Post-Training Window',rx:'No cold for 4–6 hrs after Day A/B',d:'Cold suppresses mTOR signalling — the pathway that drives muscle protein synthesis and hypertrophy. Even short dips (2–5 min) are not proven safe post-training. The blunting is dose-dependent (colder + longer = worse) but no threshold has been established (Roberts 2015, Petersen & Fyfe 2021).'},
               {n:'Safe Windows',rx:'Off days · mornings · after Zone 2',d:'OK on Recovery days, Activity days, or morning before training. Endurance / Zone 2 adaptations are unaffected — cold only blunts the strength/hypertrophy (mTOR) pathway.'}]},
      {id:'sN_heat', time:'Any', title:'Heat (Sauna / Hot Bath)',           sub:'No timing restriction',
       detail:[{n:'Heat Exposure',rx:'Post-training or evening',d:'No blunting effect on muscle adaptation. Increases blood flow and may aid recovery. No timing restriction relative to training. Use as desired.'}]},
    ],
    micro:[],
    briefing:{
      ok:['This is a reference card — no exercises, no tracking.',
          'Collagen + Creatine can be mixed in the same drink.',
          'Protein target assumes active adult in rehab/training context (Attia: 1.6–2.2 g/kg).'],
      caution:['Collagen must be separated from protein meals by 2+ hours.',
               'Cold water after Day A/B blunts strength gains — even brief exposure.'],
    }
  },
```

- [ ] **Step 2: Verify in browser**

Open index.html, tap the 🥩 button. The Diet & Supplements card should render with all 5 expandable sections: Protein, Daily Stack, Timing Rules, Cold Exposure, Heat. Tap each to expand and verify content.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "add diet BLOCKS entry with all content sections"
```

---

### Task 3: Version bump and changelog

**Files:**
- Modify: `index.html` — three version locations + changelog section

- [ ] **Step 1: Update version in all 3 locations**

Find and replace `v11.43` → `v11.5` in:

1. `<title>` tag (line ~9): `kneehab v11.5`
2. `.ms-title` in More menu: `kneehab v11.5`
3. `.cover-h1` on Home page: `kneehab<br><em>v11.5</em>`

- [ ] **Step 2: Add changelog entry**

Insert a new changelog block at the top of the changelog section (inside `<div id="overview">`, before the v11.43 entry):

```html
  <div class="changes">
    <div class="changes-title">Changes v11.5 — diet & supplements reference</div>
    <ul class="changes-list">
      <li><strong>Diet & Supplements card added (🥩).</strong> New day-type button. Static reference: protein target (140–180g/day, Attia), daily supplement stack (collagen + vit C + creatine), timing rules, cold exposure guidelines (Roberts 2015), heat exposure.</li>
    </ul>
  </div>
```

- [ ] **Step 3: Verify in browser**

Check: version shows v11.5 in title bar, More menu, and Home page. Changelog entry appears at top of Overview page.

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "v11.5: diet & supplements reference card"
```

---

### Task 4: Update memory

**Files:**
- Modify: `C:\Users\cpboa\.claude\projects\C--Users-cpboa-Dropbox-Cal-Cal-Active-Projects-kneehab\memory\MEMORY.md`

- [ ] **Step 1: Update version and day types**

Update `Current Version` to `v11.5`. Add `Diet & Supplements` to the day types list. Add `v11.5 key changes` entry. Update BLOCKS keys to include `'N'`.

- [ ] **Step 2: Commit**

```bash
git add -A
git commit -m "update memory for v11.5"
```
